# Default values for bob.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

manifest: Daemonset
image:
  pullPolicy: IfNotPresent
  repo: bobdotme
  replicas: 3
  version: latest

